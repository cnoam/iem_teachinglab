# pytest.ini
[pytest]
# Add the project root (the directory containing poll_clusters.py)
# to the Python path. This allows modules to be imported directly by name.
pythonpath = .
# Optionally, define where your tests are located:
testpaths = tests
